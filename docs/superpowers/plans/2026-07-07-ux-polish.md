# UX Polish (Plan A) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The user-approved UX review quick wins + medium items: persistent deal-context strip, draft visibility, inline missing-docs actions, all-errors footer, already-submitted deep link, success "what happens next" line. (Stale-payment bug already fixed in Plan B.)

**Architecture:** Pure presentational work inside RIPCO-UI primitives, plus one new read helper (`findDoneDealForListing`). No new writes to monday; no board structure changes. Context-strip values derive live from `form`, so they update as the broker edits.

**Tech Stack:** existing — React/TS, RIPCO-UI tokens, Vitest for the one pure helper.

## Global Constraints

- RIPCO-UI is law: StatCard per §9.10 (borderless; 10.5px uppercase muted label; 20px/500 `.num` value); status in a `Pill`, never tinted rows; light + dark both work via tokens; no `position:fixed`.
- Values render instantly from the loaded item/form (§20.2 — no placeholder chrome).
- `?mock=1` still makes zero API calls (draft autosave already skipped in mock; the finder is only called on the already-submitted path, unreachable in mock).
- Verified live 2026-07-07: ISG Listings has **no** relation column to Done Deals — resolve via Done Deals `Source Deal Link` (`board_relation_mkzzjbkt`) `linked_item_ids` scan, newest first.

---

### Task 1: Deal-context strip

**Files:** Modify `src/components/ui/primitives.tsx` (add `StatCard`), Create `src/components/ui/DealContextStrip.tsx`, Modify `src/App.tsx` (header).

- `StatCard {label, value, numeric?}` in primitives per §9.10.
- `DealContextStrip {form, dealStage}`: row under the title bar inside the sticky header — 4 stat cards: Sales price (`money(finalSalesPrice)`), Sched. commission (`money(scheduledCommission)`), Close date (short "Jun 30, 2026" format), Property type. Values live from `form`; em-dash via StatCard when empty. Deal-stage `Pill` moves into the title row next to the item name.
- Layout: `grid grid-cols-2 sm:grid-cols-4`, px-5 py-2, hairline top border; ~80px cost; renders on every wizard step (not on gate/error/success screens).

### Task 2: Draft visibility

**Files:** Modify `src/lib/donedeal/storage.ts` (`saveDraft` returns `Promise<boolean>` success), `src/App.tsx`.

- App state `lastSavedAt: Date | null`; autosave effect sets it when `saveDraft` resolves true. Header title bar right side gains muted stamp `Draft saved 2:32 PM` (12px, `text-muted`), hidden until first save.
- App state `resumedDraft: boolean` set when init used a draft. Thin banner row under the header strip: "Resuming your draft — " + `Start over` link-button + dismiss ×. Start over = `clearDraft(itemId)` + `setForm(prefillFromItem(item))` + reset to step 1 + clear `completed` + hide banner.

### Task 3: Error surfacing

**Files:** Modify `src/App.tsx` (footer), `src/components/steps/ReviewSubmit.tsx` (missing-docs links).

- Footer: replace `errors[0]` with a right-aligned stacked list of ALL current step errors (12px red, `space-y-0.5`, max-w prose). Next stays disabled with the first error as title.
- ReviewSubmit red box: each flag row gains a `Go to deductions` link (all conditional docs live on step 5) → `onEdit(5)` — §20.4 inline action.

### Task 4: Already-submitted deep link

**Files:** Modify `src/lib/donedeal/read.ts`, `src/App.tsx`; Test `src/lib/donedeal/read.test.ts` (new).

- Pure `matchDoneDealToListing(items, listingId): string | null` — items carry `{id, linked: number[]}`; returns first whose linked ids include the listing id. Unit-tested.
- `findDoneDealForListing(listingId): Promise<string | null>` — Done Deals `items_page(limit: 100, query_params: {order_by: [{column_id: "__creation_log__", direction: desc}]})` selecting `column_values(ids:["board_relation_mkzzjbkt"]) { ... on BoardRelationValue { linked_item_ids } }`; follows no cursors (newest 100 is plenty for this young board — log-noted); errors → null.
- Already-submitted screen: after phase set, fire the finder (non-blocking); when resolved show `Open Done Deal record` button → `monday.execute('openItemCard', {itemId})`.

### Task 5: Success copy + verification

**Files:** Modify `src/App.tsx` (SuccessScreen).

- Sub-line becomes: "Finance reviews new submissions on the Done Deals board — no further action needed."
- Verify: vitest + typecheck + build; extend the scratchpad browser drive: strip present on steps 1→7 with values updating after the step-3 commission edit; footer lists 2+ errors on an invalid step; review red-box shows "Go to deductions"; success screen shows the new line. Commit per task.
